<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calorie_king_stefan_rob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Fat_Secret_Set_Amount"></activity>   <!-- delete -->
        <activity android:name=".FatSecret_Search_Meal" />            <!-- delete -->
        <activity android:name=".Ingredient_Display" />               <!-- delete -->
        <activity android:name=".MyGroupsPageActivity" />
        <activity android:name=".CalorieGoalBoardActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".HomeScreenActivity" />
        <activity android:name=".SignUpPageActivity" />
        <activity android:name=".LogInActivity" />
        <activity
            android:name=".AddMealActivity"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"/>        <!-- portrait only-->
        <activity android:name=".AddPreparationActivity" />
        <activity android:name=".AddScaleReadingActivity" />
        <activity android:name=".AddWorkoutActivity" />
        <activity android:name=".DailyLogActivity" />
        <activity android:name=".MyCalorieHistoryActivity" />
        <activity android:name=".OpeningPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>